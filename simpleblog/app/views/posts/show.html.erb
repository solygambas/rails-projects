<h1 class="my-4"><%= @post.title %></h1>
<p><%= @post.body %></p>
<hr>
    <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-secondary mb-2' %>
    <%= button_to "Delete", post_path(@post), method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } }, class: 'btn btn-danger' %>